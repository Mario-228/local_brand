import 'package:flutter/material.dart';

class HomeFeatureViewBody extends StatelessWidget {
  const HomeFeatureViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}